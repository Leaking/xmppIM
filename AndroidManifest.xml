<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.XMPP"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.XMPP.Activity.Login.AppStartActivity"
            android:label="@string/appTitle" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.XMPP.Activity.Login.LoginActivity"
            android:label="@string/title_activity_login" >
        </activity>
        <activity
            android:name="com.XMPP.Activity.Mainview.MainviewActivity"
            android:label="@string/title_activity_firends_list" >
        </activity>

        <service
            android:name="com.XMPP.Service.ContactsService"
            android:enabled="true" >
        </service>
        <service
            android:name="com.XMPP.Service.ReconnectService"
            android:enabled="true" >
        </service>
        <service
            android:name="com.XMPP.Service.MessageService"
            android:enabled="true" >
        </service>

        <activity
            android:name="com.XMPP.Activity.Chatting.ChattingActivity"
            android:label="@string/title_activity_chatting" >
        </activity>
        <activity
            android:name="com.XMPP.Activity.ChatRoom.ChatRoomActivity"
            android:label="@string/title_activity_chat_room"
            android:windowSoftInputMode="stateHidden" >
        </activity>
    </application>

</manifest>