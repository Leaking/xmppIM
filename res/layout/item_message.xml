<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:padding="@dimen/recyclerView_padding" >

    <ImageView
        android:id="@+id/messageIcon"
        android:layout_width="@dimen/message_icon_size"
        android:layout_height="@dimen/message_icon_size"
        android:layout_marginRight="10dip" />

    <RelativeLayout
        android:layout_width="0dip"
        android:layout_height="match_parent"
        android:layout_weight="1" >

        <TextView
            android:id="@+id/title"
            style="@style/msg_title"
            android:layout_alignParentTop="true" />

        <TextView
            android:id="@+id/previewWords"
            style="@style/msg_previewWords"
            android:layout_alignParentBottom="true" />
    </RelativeLayout>

    <TextView
        android:id="@+id/timeStamp"
        style="@style/msg_timeStamp" />

</LinearLayout>